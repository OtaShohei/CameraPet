<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Buttonが押された時 -->
    <item android:drawable="@drawable/set_twt_btn" android:state_pressed="true"/>
    <!-- 色を変更する場合は、android:color="@color/red" のようにする -->

    <!-- Buttonが押されてない時 -->
    <item android:drawable="@drawable/twt_btn" android:state_pressed="false"/>

</selector>